# Supabase — vul in met je eigen project (cloud) of lokale supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server-only (NIET in de browser): voor admin-acties / seeding
SUPABASE_SERVICE_ROLE_KEY=

# AI-mailer (optioneel) — zonder key valt de mailer terug op sjablonen
ANTHROPIC_API_KEY=
# DETAVIA_AI_MODEL=claude-haiku-4-5-20251001

# E-mail versturen (optioneel) — facturen + AI-mailer via Resend
RESEND_API_KEY=
